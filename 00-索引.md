# GitHubPicks 项目索引

## 基本信息

- 项目名称：GitHubPicks
- 项目类型：英文独立站 / GitHub 项目精选目录
- 当前阶段：MVP 上线前优化
- 正式域名：`https://github.next-happy.com`
- 技术栈：Next.js App Router + TypeScript + Tailwind CSS
- 数据方式：JSON 文件驱动，构建时静态生成
- 部署平台：Vercel
- GitHub 账号：`pengyunzhaoisme1207-bit`

## 项目定位

GitHubPicks 面向普通用户和轻技术用户，用场景化语言解释优质 GitHub 项目能解决什么问题。站点不是 GitHub README 的复制集合，也不是自动抓取榜单，而是人工筛选、编辑改写、按使用场景组织的开源项目推荐站。

核心价值：

- 帮用户快速判断一个开源项目是否值得尝试
- 把开发者语言翻译成普通人能理解的使用场景
- 为 next-happy.com 站群补充开源工具发现入口
- 长期承接 SEO 流量和 AdSense 变现

## 必读文件

任何 Agent 开始工作前必须读取：

1. `AGENTS.md`
2. `README.md`
3. `CLAUDE.md`
4. `/Users/jacky.peng/Obsidian/产品制造工厂/仓库最高指导思想指南/README.md`

## 关键目录

- `src/app/`：页面路由、metadata、sitemap、robots
- `src/components/`：页面区块和 UI 组件
- `src/lib/`：数据读取、类型、常量、工具函数
- `data/`：项目、分类、每周精选内容

## 当前状态

- 已有 42 个项目条目
- 已有 10 个分类
- 已有 About / Contact / Privacy / Submit 基础页面
- 已有 sitemap 和 robots
- 已完成基础项目页、分类页、Weekly Picks 页面

## 近期优先级

1. 保持内容质量，不盲目堆项目数量
2. 每次新增项目时检查原创性、场景具体性、内部链接和页面厚度
3. 每周更新 `data/weekly-picks.json`
4. 上线前确认 AdSense Auto Ads 或真实广告位 ID，不使用假 slot
5. 部署前运行 `npm run lint` 和 `npm run build`

## 协作红线

- 不覆盖用户或其他 Agent 的未提交改动
- 不提交 `.next`、`node_modules`、`.env*`、`.vercel`
- 不把密钥、Token、API Key 写进前端代码
- 不直接复制 GitHub README 作为页面正文
- 不上线 TODO、占位广告、空页面、假链接

## 验证命令

```bash
npm run lint
npm run build
git status --short
```

如果 `npm run build` 在 Agent 沙箱里报 Turbopack `binding to a port` / `Operation not permitted`，需要在真实终端或已授权环境复测。
